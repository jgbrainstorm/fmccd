#! /usr/bin/env python 
import sys
sys.path.append('/home/jghao/fmccd')
if len(sys.argv) == 1:
    print 'syntax: sub_img imagefile biasfile targetfile'
    print '        the resulting image will supercede the image file'
else:
    from fermiMCCD import *
    t=Img_sub(sys.argv[1],sys.argv[2])

